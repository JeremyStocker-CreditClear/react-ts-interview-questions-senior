import React from "react";
import "./TestComponent2.css";

/**
 * === TEST ===
 * Create an input field which works when you type into it, make it look good. Also use component
 * props with fields like `value`, `placeholder` and whatever else is relevant.
 */
const TestComponent2: React.FC = () => {
  return null;
};

export { TestComponent2 };
